<!-- Keep it short. Link the issue this closes, if any. -->

## What this changes

## Why

## Checklist

- [ ] `make fmt-check vet test` passes
- [ ] New behavior has a test (unit tests do not hit the network)
- [ ] If an operation's behavior changed, the prompt file and its docs are updated
- [ ] If `pkg/types` changed, the `--json` docs are updated and the change is backward-compatible
